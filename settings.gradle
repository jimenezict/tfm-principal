pluginManagement {
    repositories {
        mavenLocal()
        mavenCentral()
        gradlePluginPortal()
        maven {
            url 'http://repo.spring.io/plugins-release'
        }
    }
}

rootProject.name = 'principal'
